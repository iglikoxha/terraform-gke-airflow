terraform {
  # TODO Add required version
  required_providers {
    google = {
      source = "hashicorp/google"
      # TODO Add required version
    }
    google-beta = {
      source = "hashicorp/google-beta"
      # TODO Add required version
    }
  }
}
